<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lehiteixeira.banco_neon">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".NeonApplication"
        android:allowBackup="true"
        android:icon="@mipmap/neon_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/neon_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Activity.MainActivity" />
        <activity
            android:name=".View.Activity.SplashActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activity.TransferLogActivity"></activity>
        <activity android:name=".View.Activity.SendMoneyActivity"></activity>
    </application>

</manifest>